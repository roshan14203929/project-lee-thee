# Project guideline overrides

Copy only rules that differ from the kit defaults into this directory. Use the
same role filenames as `guidelines/global/qa/` and `guidelines/builder.md` /
`guidelines/extractor.md`. Page-specific deltas belong under the page's own
`guidelines/` directory.

